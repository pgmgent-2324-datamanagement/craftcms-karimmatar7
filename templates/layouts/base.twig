<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ entry.title|default('') }}</title>
    <link rel="stylesheet" href="../css/admin.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
</head>
<body style="display: flex; flex-direction: column; min-height: 100vh;">
    <header class="d-flex justify-content-between align-items-center p-3 p-3 bg-warning">
        <a class="text-decoration-none text-dark fs-3" href="/"><strong>Karim Webstore</strong></a>
        <ul class="list-unstyled d-flex align-items-center" style="gap: 1rem;">
            {% set categories = craft.categories.group('productCategories').all() %}
            {% for category in categories %}
                <li>
                    <a href="{{ category.url }}" class="text-decoration-none text-dark fs-4">
                        {{ category.title }}
                    </a>
                </li>
            {% endfor %}
        </ul>
        <nav class="d-flex gap-3">
            {% for entry in navigation.navigation %}
                <a class="text-decoration-none text-dark fs-4" href="{{ entry.navlink[0].url }}"><strong>{{ entry.navname }}</strong></a>
            {% endfor %}
        </nav>
    </header>

    <div class="container" style="flex: 1;">
        <div class="row">
            <div class="col-12 py-4">
                {% block body %}
                {% endblock %}
            </div>
        </div>
    </div>

    <footer class="text-center bg-warning p-3 p-3">
        Copyright © Karim Matar - {{ now|date("Y") }}
    </footer>
</body>
</html>
